# Beckford Moves (Next.js)

Production deploy: **Vercel** connected to **`ackersml/beckford-moves`**.

- **Deploy & env**: see **[DEPLOY.md](./DEPLOY.md)** (Keystatic, OAuth callbacks, YAML layout).  
- **Env template**: copy **`env.example`** → `.env.local` for local development (never commit secrets).
