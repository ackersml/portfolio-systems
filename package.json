{
  "name": "portfolio-systems",
  "version": "1.0.0",
  "description": "Platform builder website with affiliate program",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "vercel dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "resend": "^6.6.0",
    "stripe": "^14.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
